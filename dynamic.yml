tls:
  stores:
    default:
      defaultCertificate:
        certFile: "/certs/dev.localhost.cert"
        keyFile: "/certs/dev.localhost.key"
  certificates:
    - certFile: "/certs/dev.localhost.cert"
      keyFile:  "/certs/dev.localhost.key"